// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.0.0"  // Gunakan versi terbaru
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.22"  // Gunakan versi terbaru
    }
}

plugins {
    alias(libs.plugins.android.application) apply false
}
